{
  "extends": "./tsconfig.json",
  "exclude": [
    "${configDir}/**/*.config.*",
    "${configDir}/**/*.test.*",
    "${configDir}/**/.air/**",
    "${configDir}/**/.git/**",
    "${configDir}/**/.make/**",
    "${configDir}/**/.ruff_cache/**",
    "${configDir}/**/.swc/**",
    "${configDir}/**/build/**",
    "${configDir}/**/dist/**",
    "${configDir}/**/node_modules/**",
    "${configDir}/**/persistent/**",
  ],
}
